import React from "react";
import Video from "next-video";

const PresentationVideo = () => {
  return (
    <>
      <section className="mb-10">
        {/* <Video src={} autoPlay muted loop playsInline controls={false} /> */}
      </section>
    </>
  );
};

export default PresentationVideo;
